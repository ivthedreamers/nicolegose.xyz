// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://thedreamerlab.co',
  // Custom domain at the apex → served from root, no base path needed.
});
